// Slice-local presentation data for Hiburan & Gaya Hidup (concierge + calendar).

export const DAYS = ["Sen", "Sel", "Rab", "Kam", "Jum", "Sab", "Min"];

// Illustrative/demo constants relocated to the central mock-data store.
// Re-exported here so existing importers (screens import from "./data") keep working.
export { CALENDAR_EVENTS, STATS } from "@/mock-data/hiburan-gaya-hidup";
